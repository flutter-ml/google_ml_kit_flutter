name: google_ml_kit_face_detection
description: A Flutter Plugin for ML Kit - Face detection.
version: 0.0.1
homepage: https://github.com/bharat-biradar/Google-Ml-Kit-plugin
publish_to: none # TODO: remove once split is ready

environment:
  sdk: ">=2.16.0 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  google_ml_kit_commons:
    path: ../google_ml_kit_commons

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
  plugin:
    platforms:
      android:
        package: com.google_ml_kit_face_detection
        pluginClass: GoogleMlKitFaceDetectionPlugin
      ios:
        pluginClass: GoogleMlKitFaceDetectionPlugin
